# 初期セットアップ

```sh
$ ruby -v
    ruby 3.4.1 (2024-12-25 revision 48d4efcb85) +PRISM [x86_64-linux]

$ rails -v
    Rails 8.0.1

$ sudo apt-get install postgresql postgresql-contrib libpq-dev

$ rails new backend-src --api --database=postgresql

```
